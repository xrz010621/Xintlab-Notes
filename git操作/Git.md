
```bash
git fetch upstream dev              [获取远程dev最新信息]

git stash push -u -m "before switch upstream dev" [引号里是commit内容，保存stash]

git switch -c weijewu-new-apm32-code "upstream/weijewu/embd-27-apm32-can-ota-全链路验证(第三版,验证功能,单标标定算法)"  [前面是新的分支名，后面是目标分支名]

```
